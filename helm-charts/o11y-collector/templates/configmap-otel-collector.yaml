{{ if and .Values.otelCollector.enabled (or .Values.metricsEnabled .Values.tracesEnabled) }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "splunk-otel-connector.fullname" . }}-otel-collector
  labels:
    app: {{ template "splunk-otel-connector.name" . }}
    chart: {{ template "splunk-otel-connector.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
data:
  relay: |
    {{- $config := include "splunk-otel-connector.otelCollectorConfig" . | fromYaml }}
    {{- .Values.otelCollector.config | mustMergeOverwrite $config | toYaml | nindent 4 }}
{{- end }}
