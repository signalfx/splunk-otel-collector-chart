clusterName: CHANGEME
splunkObservability:
  realm: CHANGEME
  accessToken: CHANGEME

targetallocator:
  enabled: true
  targetAllocator:
    serviceAccount:
      create: true
    imagePullSecrets:
      - my-registry-secret
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 250m
        memory: 512Mi
