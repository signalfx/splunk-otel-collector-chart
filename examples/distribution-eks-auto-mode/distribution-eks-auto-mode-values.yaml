splunkObservability:
  realm: CHANGEME
  accessToken: CHANGEME

distribution: eks/auto-mode
cloudProvider: aws

# Enabling the gateway is recommended when deploying to eks/fargate
gateway:
  enabled: true
