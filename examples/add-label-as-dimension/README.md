# Example of chart configuration

## Adding a label as a dimension

This example is an advanced use case where a user wants to promote a label from a property to a dimension
so they may tag additional metadata on the dimension.
